import type { TungaConfig } from "../types/index.js";
const events=new Set(["click","change","submit","keydown","keyup","focus","blur","input","load","error"]); const methods=new Set(["GET","POST","PUT","PATCH","DELETE","HEAD","OPTIONS"]);
export function shouldIgnoreString(raw:string, config:TungaConfig): string | undefined { const s=raw.trim(); if (s.length < config.filters.minLength) return "too short"; if (config.filters.ignorePunctuationOnly && /^[\p{P}\p{S}\s]+$/u.test(s)) return "punctuation only"; if (config.filters.ignoreNumbers && /^[\d\s.,:-]+$/.test(s)) return "numeric"; if (/^https?:\/\//.test(s)||/^mailto:/.test(s)) return "url"; if (config.filters.ignoreRoutes && /^(\/|\.\/|\.\.\/)[\w./:-]*$/.test(s)) return "route or path"; if (/^[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}$/.test(s)) return "email"; if (/^#[0-9a-fA-F]{3,8}$/.test(s)) return "color"; if (/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(s)) return "uuid"; if (methods.has(s)) return "http method"; if (/^[\w.+-]+\/[\w.+-]+$/.test(s)) return "mime type"; if (/^[A-Z][A-Z0-9_]+$/.test(s)) return "environment variable"; if (events.has(s)) return "event name"; if (config.filters.ignoreClassNames && looksLikeClassName(s)) return "class name"; if (/^[a-z][\w-]*(\.[a-z][\w-]*)+$/.test(s)) return "dotted key"; return undefined; }
function looksLikeClassName(s:string){ const parts=s.split(/\s+/); return parts.length>1 && parts.every(p=>/^-?([a-z][\w-]*:)*(text|font|bg|border|p|m|px|py|mx|my|w|h|flex|grid|items|justify|rounded|shadow|gap|space|block|inline|hidden|container|opacity|z|overflow|relative|absolute|fixed|sticky)-[a-z0-9_\-[\]/.%#]+$/i.test(p) || /^(flex|grid|block|inline-block|hidden|relative|absolute|fixed|sticky|sr-only)$/.test(p)); }
export function confidenceFor(text:string){ return text.length>40 ? "medium" as const : "high" as const; }
