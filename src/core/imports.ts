import * as t from "@babel/types";
export function ensureImport(program:t.Program, opts:{functionName:string; importSource:string; importKind:"named"|"default"}){ for(const n of program.body){ if(t.isImportDeclaration(n)&&n.source.value===opts.importSource){ if(opts.importKind==="default"){ if(!n.specifiers.some(s=>t.isImportDefaultSpecifier(s)&&s.local.name===opts.functionName)) n.specifiers.unshift(t.importDefaultSpecifier(t.identifier(opts.functionName))); } else if(!n.specifiers.some(s=>t.isImportSpecifier(s)&&t.isIdentifier(s.imported,{name:opts.functionName}))) n.specifiers.push(t.importSpecifier(t.identifier(opts.functionName),t.identifier(opts.functionName))); return; } } const decl=opts.importKind==="default"? t.importDeclaration([t.importDefaultSpecifier(t.identifier(opts.functionName))],t.stringLiteral(opts.importSource)): t.importDeclaration([t.importSpecifier(t.identifier(opts.functionName),t.identifier(opts.functionName))],t.stringLiteral(opts.importSource)); program.body.unshift(decl); }
